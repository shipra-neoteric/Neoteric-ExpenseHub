# Local dev: leave unset — Vite's dev-server proxy (vite.config.js) forwards
# /api to http://localhost:4000 automatically.
#
# Production (Vercel): set this to your deployed Render API URL, e.g.
# VITE_API_BASE_URL=https://expensehub-api.onrender.com/api
VITE_API_BASE_URL=
